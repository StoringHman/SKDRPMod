# SKDRPMod
Helps keep SKDRP Mod Menu's up-to-date and implements other checks. Features will only work if the respective games have HTTPS requests enabled.

This is basically just data that the menu grabs on server start, and then uses respectively. (eg: CurrentModVersion and ModMenuVersion)
